package ross.enums;


public enum FlightStatus {
    CHECK_IN, //can't type "check-in"
    GATE_CLOSED,
    ARRIVED,
    DEPARTED_AT,
    UNKNOWN,
    CANCELED,
    EXPECTED_AT,
    DELAYED,
    IN_FLIGHT
}
